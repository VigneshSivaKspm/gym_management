// src/api/auth/login.ts
// DEPRECATED: Use firebaseAuth service directly
// Kept for reference - user login is now handled by frontend service

import { loginUser, loginTrainee } from "../../services/firebaseAuth";

export { loginUser, loginTrainee };
