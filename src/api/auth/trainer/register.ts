// src/api/auth/trainer/register.ts
// DEPRECATED: Use firebaseAuth service directly
// Kept for reference - trainer registration is now handled by frontend service

import { registerTrainer } from "../../../services/firebaseAuth";

export { registerTrainer };
