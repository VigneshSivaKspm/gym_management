// src/api/auth/admin/register.ts
// DEPRECATED: Use firebaseAuth service directly
// Kept for reference - admin registration is now handled by frontend service

import { registerAdmin } from "../../../services/firebaseAuth";

export { registerAdmin };
